import 'package:flutter/material.dart';

class MainColors
{
    MainColors._();

    // App Basic Colors
    static const Color appbarbackground = Color(0xFFF5F5F5);
    static const Color appbackground = Color(0xFFfafafa);
    static const Color navbackground = Color(0xFFFFFFFF);

    // Buttons`
    static const Color buttonsbackgroundred = Color(0xFFDC143C);

}